draw_sprite_ext(sprite_index, image_index, x, y, image_xscale, image_yscale, 0, c_white, 0.1);
/* //temporary
for (var i = 0; i < array_length(buttonArray); i ++){
		draw_sprite(spr_empty, 0, buttonArray[i].x, buttonArray[i].y);
}