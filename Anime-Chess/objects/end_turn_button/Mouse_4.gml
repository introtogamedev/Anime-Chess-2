var transition_puff = instance_create_layer(0, 0, "Transitions", obj_transition_puff, {spd : 50});

//timer set for the transition to be executed. 
//then, the turn transition will be executed. 
alarm[0] = 40;